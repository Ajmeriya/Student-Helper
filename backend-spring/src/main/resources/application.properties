# Server Configuration
server.port=${SERVER_PORT:5000}

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:Student_Helper}
spring.datasource.username=${MYSQL_USERNAME:root}
spring.datasource.password=${MYSQL_PASSWORD:Harsh@101845}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}

# JWT Configuration
jwt.secret=${JWT_SECRET:your-secret-key-change-in-production-minimum-64-characters-required-for-hs512-algorithm}
jwt.expiration=${JWT_EXPIRATION:1728000000}

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:du2q7sfps}
cloudinary.api_key=${CLOUDINARY_API_KEY:355375538348748}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:-p9Bc__Ci8rW43vlPDdkLZzIq6U}

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.file-size-threshold=2KB

# Logging Configuration
logging.level.com.studenthelper=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN

# Payment Configuration (Dummy Mode)
# Set PAYMENT_MODE=dummy for demo/testing, or PAYMENT_MODE=real for production with real payment gateway
payment.mode=${PAYMENT_MODE:dummy}

