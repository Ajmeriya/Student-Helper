# Server Configuration
server.port=${SERVER_PORT:5000}

# Load local env file for development (Spring does not auto-load .env by default)
spring.config.import=optional:file:.env[.properties],optional:file:./backend-spring/.env[.properties]

# Profile Configuration - Development
spring.profiles.active=dev

# Database (Docker + Local compatible)
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/student_helper
spring.datasource.username=root
spring.datasource.password=Harsh@101845

spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}

# JWT
jwt.secret=${JWT_SECRET:your-secret-key-change-in-production}
jwt.expiration=${JWT_EXPIRATION:1728000000}

# Cloudinary
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:du2q7sfps}
cloudinary.api_key=${CLOUDINARY_API_KEY:355375538348748}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:-p9Bc__Ci8rW43vlPDdkLZzIq6U}


# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Payments
payment.mode=${PAYMENT_MODE:dummy}

# SMTP (for email verification)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:ajmeriyaharsh324@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Google OAuth client IDs (comma-separated)
google.oauth.client-ids=${GOOGLE_CLIENT_IDS:}

# Multipart upload limits (PG/Hostel images/videos)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
server.tomcat.max-swallow-size=-1
